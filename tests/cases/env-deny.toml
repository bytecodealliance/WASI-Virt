component = "get-env"

[host_env]
PRIVATE_TOKEN = "PRIVATE"
PUBLIC_VAR = "VAL"

[virt_opts.env]
overrides = [["CUSTOM", "VAL"]]

[virt_opts.env.host]
deny = ["PRIVATE_TOKEN"]

[expect]
env = [["CUSTOM", "VAL"], ["PUBLIC_VAR", "VAL"]]
